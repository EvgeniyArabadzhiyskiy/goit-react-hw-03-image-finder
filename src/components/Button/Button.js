const Button = ({onClickBtnIncrement}) => {
    return ( <div>
        <button className="Button" type="buttton" onClick={onClickBtnIncrement}>Load more</button>
    </div> );
}
 
export default Button;